# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
database-debug.log
storage-debug.log
pubsub-debug.log

# .firebaserc is committed — it names the project this app deploys to.

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node (only used for optional local tooling — the app itself has no build step)
node_modules/
npm-debug.log*

# Local data exports / backups
backups/
*.local.csv
